# {{TASK_TITLE}} — {{FEATURE_SLUG}}

This directory contains phase-specific mono-spec prompt templates. The active
workflow renders one of the step templates named in `workflow.yaml`.

{{include:rules/global_rules.md}}
