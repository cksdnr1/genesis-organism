# Encounter revision — total-spec validation

Reviewer: active Codex operator self-review. Scope: readiness to revise the
conditional phase plan, not runtime conformance or acceptance of D01–D14 choices.

Findings first:

- No blocker for downstream planning. Machine Encounter is now the leading
  product scenario rather than an appendix to identity/replay infrastructure.
- Medium retained execution gate: the eight receipt fields are candidate
  responsibilities. D13 still needs exact versioning, authorization, result
  terminality and acyclic bindings before implementation.
- Medium retained execution gate: PhenotypeState representation and typed
  capability successor units/frames/evidence are D07 work; JSON files stay unchanged.
- Medium retained research boundary: D14 needs justified selection measures,
  controls and population outcomes before Darwinian claims. No death or arbitrary
  fitness mechanism has been introduced.
- Low editorial defect corrected: D13/D14 rows now continue the decision table
  rather than appearing after a blank line as an unheaded Markdown table.

Readiness: **96/100 for downstream planning**. Score rubric: source/claim scope
20/20; preserved invariants/history 20/20; structured compatibility 20/20;
encounter/causal-loop focus 20/20; implementation decision specificity 16/20.
The last deduction reflects explicit future contracts, not a hidden protocol approval.

| Risk class | Review evidence |
| --- | --- |
| Missing entry points | Baseline has no organism runtime; future interface work remains conditional |
| Stale assumptions | Origin and prior planning commits identified; previous results not reused as revision approval |
| Unverified behavior | Author-described prior art separated from implementation proof; no scores asserted as measurements |
| Structured vocabulary/hash mismatch | Original schema IDs/required names and JSON bytes preserved; candidate receipt mappings explicitly incompatible where needed |
| Deterministic inputs | Required model-output bytes retained/available; digest alone insufficient; no model regeneration during replay |
| File/path compatibility | Existing total spec/phase plan paths retained; result uses encounter_revision_result.md in revision task |
| Future leakage | No source/dependency/new canonical schema, child execution, actual ecology run or birth |

Checks: H00–H28 and R01–R12 mappings present; all original schema IDs and required
names present; original observer/phenotype JSON, ORIGIN.md and unborn placeholder
byte-identical to baseline; diagram distinguishes evidence/admission/outcome;
D13/D14 explicitly deferred; five-axis review has bounded primary-source findings.

Required phase-plan changes: early encounter requirements, separate D07/D13
contract work, receipt-to-admission implementation, a later-expression causal
control milestone, and post-lineage population/ecology decision and experiment.
Do not rewrite old completion records or silently remap old execution numbers.

Exact gate result: `approved` for this revision's total_spec_validate.
